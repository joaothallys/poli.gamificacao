import { NextPage } from "next";

const Learn: NextPage = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen bg-gray-50">
      <h1 className="text-3xl font-bold text-gray-800 mb-4">Tela não encontrada</h1>
      <p className="text-gray-600 text-lg">A página que você está tentando acessar não existe ou foi removida.</p>
    </div>
  );
};

export default Learn;